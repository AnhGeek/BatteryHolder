## macOS
.DS_Store

## Xcode
build/
DerivedData/
xcuserdata/
*.xcuserstate
*.moved-aside
*.xcworkspace/xcuserdata/
# Keep the committed project + shared scheme, ignore per-user state only.
BatteryHolder.xcodeproj/project.xcworkspace/xcuserdata/
BatteryHolder.xcodeproj/xcuserdata/

## Swift Package Manager
.build/
.swiftpm/
Packages/
Package.resolved

## CocoaPods / Carthage (if ever added)
Pods/
Carthage/Build/

## AWS SAM
backend/.aws-sam/
samconfig.toml

## Secrets
*.pem
*.p12
.env

## App signing — the keystore and its passwords live outside the repo, in
## Documents/BatteryHolder-keys. Losing them means never being able to update
## the published app; committing them means anyone can publish as us.
*.jks
*.keystore
key.properties
